# Service Management API

Универсальный веб-интерфейс для управления systemd сервисами через HTTP API.

**🚀 Быстрый старт:** `sudo ./deploy.sh`

## Возможности

- Получение статуса сервиса
- Запуск сервиса
- Остановка сервиса
- Перезагрузка сервиса
- Защита через TOKEN параметр
- Защита от инъекций

## Использование

### Получение статуса сервиса
```
GET /servise/status/{service_name}?TOKEN=your_token
```

### Запуск сервиса
```
GET /servise/start/{service_name}?TOKEN=your_token
```

### Остановка сервиса
```
GET /servise/stop/{service_name}?TOKEN=your_token
```

### Перезагрузка сервиса
```
GET /servise/restart/{service_name}?TOKEN=your_token
```

## Примеры

```bash
# Статус сервиса
curl "https://your-domain.com/servise/status/nginx.service?TOKEN=your_secret_token"

# Перезагрузка сервиса
curl "https://your-domain.com/servise/restart/nginx.service?TOKEN=your_secret_token"
```

## Требования к серверу

### Обязательные:
- Linux с systemd (Ubuntu 18.04+, Debian 10+, CentOS 7+)
- Python 3.7 или выше
- python3-pip и python3-venv
- Права root (sudo) для установки
- Пользователь www-data
- Свободный порт (по умолчанию 5000)

### Опциональные:
- Nginx или Apache2 (для работы через домен)
- SSL сертификат (для HTTPS)

**Подробные требования:** см. `REQUIREMENTS.md`

## Установка

```bash
sudo ./deploy.sh
```

Скрипт автоматически выполнит все необходимые шаги установки.

## Удаление

```bash
sudo ./deploy.sh --uninstall
```

Или используйте альтернативные варианты:
- `sudo ./deploy.sh --remove`
- `sudo ./deploy.sh -u`
- `sudo ./deploy.sh -r`

Скрипт удалит:
- Systemd сервис
- Файлы проекта из `/opt/service-management`
- Файл sudoers
- Все связанные компоненты

**Примечание:** Конфигурация веб-сервера (Nginx/Apache) не удаляется автоматически.

**Подробная инструкция:** см. `INSTALL.md`  
**Частые вопросы:** см. `FAQ.md`

## Справка

```bash
sudo ./deploy.sh --help
```

Покажет все доступные команды и параметры.

### Настройка параметров

#### Настройка веб-сервера
Скрипт покажет примеры конфигурации для Nginx/Apache. Вы сами настраиваете веб-сервер с нужным доменом.

#### Порт
По умолчанию используется порт 5000. Если порт занят, скрипт автоматически найдет свободный порт.

Вы можете задать порт вручную через переменную окружения:
```bash
export SERVICE_MANAGEMENT_PORT=5001
sudo ./deploy.sh
```

#### Токен
Токен можно задать через переменную окружения или скрипт сгенерирует его автоматически:
```bash
export SERVICE_MANAGEMENT_TOKEN=your_secret_token
sudo ./deploy.sh
```

## Безопасность

- Все запросы требуют валидный TOKEN
- Имена сервисов валидируются (только буквы, цифры, дефисы, точки)
- Команды systemctl выполняются безопасно через subprocess с валидацией

